---
title: "validateJsonContent"
icon: "function"
description: "Validates JSON content to ensure it's safe to parse."
---

import { PageLink } from '/snippets/tsdocs/PageLink.jsx';
import { RefLink } from '/snippets/tsdocs/RefLink.jsx';

# validateJsonContent

## Summary

Validates JSON content to ensure it&#39;s safe to parse.  
## Signature

```typescript
static validateJsonContent(jsonString: string, options?: {
        skipPatternCheck?: boolean;
    }): string;
```

## Parameters

### jsonString
**Type**:`string`

The JSON string to validate

### options
**Type**:

```typescript
{
        skipPatternCheck?: boolean;
    }
```

**Optional**: Yes

Validation options

## Returns

**Type**:`string`

Validates JSON content to ensure it's safe to parse.