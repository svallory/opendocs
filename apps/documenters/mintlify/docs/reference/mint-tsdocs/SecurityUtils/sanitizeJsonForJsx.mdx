---
title: "sanitizeJsonForJsx"
icon: "function"
description: "Sanitizes JSON data for safe embedding in JSX components. Prevents JSON injection and ensures proper formatting."
---

import { PageLink } from '/snippets/tsdocs/PageLink.jsx';
import { RefLink } from '/snippets/tsdocs/RefLink.jsx';

# sanitizeJsonForJsx

## Summary

Sanitizes JSON data for safe embedding in JSX components. Prevents JSON injection and ensures proper formatting.  
## Signature

```typescript
static sanitizeJsonForJsx(data: unknown): string;
```

## Parameters

### data
**Type**:`unknown`

The data to serialize and sanitize

## Returns

**Type**:`string`

Sanitizes JSON data for safe embedding in JSX components. Prevents JSON injection and ensures proper formatting.