---
title: "sanitizeYamlText"
icon: "function"
description: "Sanitizes text for safe use in YAML frontmatter. Prevents YAML injection and ensures proper formatting."
---

import { PageLink } from '/snippets/tsdocs/PageLink.jsx';
import { RefLink } from '/snippets/tsdocs/RefLink.jsx';

# sanitizeYamlText

## Summary

Sanitizes text for safe use in YAML frontmatter. Prevents YAML injection and ensures proper formatting.  
## Signature

```typescript
static sanitizeYamlText(text: string): string;
```

## Parameters

### text
**Type**:`string`

The text to sanitize

## Returns

**Type**:`string`

Sanitizes text for safe use in YAML frontmatter. Prevents YAML injection and ensures proper formatting.