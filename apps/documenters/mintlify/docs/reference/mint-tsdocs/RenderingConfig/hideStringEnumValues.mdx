---
title: "hideStringEnumValues"
icon: "file"
description: "Whether to hide the value column in string enum member tables. For string enums, the value is usually the same as the member name."
---

import { PageLink } from '/snippets/tsdocs/PageLink.jsx';
import { RefLink } from '/snippets/tsdocs/RefLink.jsx';

# hideStringEnumValues

## Summary

Whether to hide the value column in string enum member tables. For string enums, the value is usually the same as the member name.  

## Description

Whether to hide the value column in string enum member tables. For string enums, the value is usually the same as the member name.