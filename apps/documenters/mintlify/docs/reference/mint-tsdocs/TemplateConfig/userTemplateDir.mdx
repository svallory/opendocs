---
title: "userTemplateDir"
icon: "file"
description: "Directory containing custom template overrides. Templates are written in Liquid format and override default templates. Use mint-tsdocs customize -t ./templates to initialize templates."
---

import { PageLink } from '/snippets/tsdocs/PageLink.jsx';
import { RefLink } from '/snippets/tsdocs/RefLink.jsx';

# userTemplateDir

## Summary

Directory containing custom template overrides. Templates are written in Liquid format and override default templates. Use `mint-tsdocs customize -t ./templates` to initialize templates.  

## Description

Directory containing custom template overrides. Templates are written in Liquid format and override default templates. Use to initialize templates.



## Examples

### Example 1

```typescript
&quot;./templates&quot;
```