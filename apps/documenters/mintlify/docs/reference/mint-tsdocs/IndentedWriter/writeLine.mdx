---
title: "writeLine"
icon: "function"
description: "A shorthand for writing an optional message, followed by a newline. Indentation is applied following the semantics of IndentedWriter.write()."
---

import { PageLink } from '/snippets/tsdocs/PageLink.jsx';
import { RefLink } from '/snippets/tsdocs/RefLink.jsx';

# writeLine

## Summary

A shorthand for writing an optional message, followed by a newline. Indentation is applied following the semantics of IndentedWriter.write(). 
## Signature

```typescript
writeLine(message?: string): void;
```

## Parameters

### message
**Type**:`string`
**Optional**: Yes



## Returns

**Type**:`void`

A shorthand for writing an optional message, followed by a newline. Indentation is applied following the semantics of IndentedWriter.write().