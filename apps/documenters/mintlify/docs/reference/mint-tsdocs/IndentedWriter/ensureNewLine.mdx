---
title: "ensureNewLine"
icon: "function"
description: "Adds a newline if the file pointer is not already at the start of the line (or start of the stream)."
---

import { PageLink } from '/snippets/tsdocs/PageLink.jsx';
import { RefLink } from '/snippets/tsdocs/RefLink.jsx';

# ensureNewLine

## Summary

Adds a newline if the file pointer is not already at the start of the line (or start of the stream). 
## Signature

```typescript
ensureNewLine(): void;
```

## Parameters

*This method takes no parameters.*

## Returns

**Type**:`void`

Adds a newline if the file pointer is not already at the start of the line (or start of the stream).